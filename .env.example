# LLM Configuration (optional — fallback report generated without it)
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1

# GitHub Token (optional — increases rate limit from 60 to 5000 req/hr)
GITHUB_TOKEN=

# Analysis Limits
MAX_FILES=30
MAX_FILE_SIZE=50000
